{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "AllowedOrigins": []
  },
  "ConnectionStrings": {
    "//": "Set via user secrets or environment variables. Never commit credentials (SOW 3 - Security). Locally: dotnet user-secrets set \"ConnectionStrings:DataIntelligenceDb\" \"<connection string>\" --project src\\DataIntelligence.Api",
    "DataIntelligenceDb": "",
    "//DataIntelligenceDbReadOnly": "Optional. A login in the di_ai_readonly role - SELECT on analytics.* only, DENY on sec and ai - that the assistant executes generated SQL as. Leave empty on a Windows-authentication-only instance, where that role has no login to authenticate as; the assistant then reaches the same restricted principal with EXECUTE AS Assistant:ExecuteAsUser. It never falls back to this file's read-write connection.",
    "DataIntelligenceDbReadOnly": ""
  },
  "Assistant": {
    "//": "ApiKey is never committed. A DeepSeek platform key (sk-...). Set it with: dotnet user-secrets set \"Assistant:ApiKey\" \"<key>\" --project src\\DataIntelligence.Api",
    "BaseUrl": "https://api.deepseek.com/",
    "//Model": "GET https://api.deepseek.com/models lists what a key can reach: deepseek-v4-flash and deepseek-v4-pro. Note the id has no vendor prefix - 'deepseek/deepseek-v4-flash' is the OpenRouter spelling and is rejected here.",
    "Model": "deepseek-v4-flash",
    "RequestTimeoutSeconds": 30,
    "SqlExecutionTimeoutSeconds": 10,
    "//MaxOutputTokens": "Analytical queries ('which month changed the most') nest three levels of derived table and run to several hundred tokens of SQL before the explanation begins. At 1024 the JSON wrapping them was truncated and the turn was recorded as RejectedUnreadableResponse - the tell is a completion count sitting exactly on this number. A ceiling, not a reservation: short answers still cost short-answer tokens.",
    "MaxOutputTokens": 3000,
    "//ExecuteAsUser": "Database user switched to before generated SQL runs, used when DataIntelligenceDbReadOnly is empty. Must belong to di_ai_readonly. Created by database-schema.sql section 6.",
    "ExecuteAsUser": "di_ai_user"
  }
}
