{
  "//Logging": "Every key under LogLevel is parsed as a log level, so this section cannot carry '//' comments the way the others do. EntityFrameworkCore.Database.Command logs every statement with its parameters, and HttpClient four lines per outbound call - both at Information, both loud enough to bury what you are reading. Set either to Information to turn it back on.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning",
      "System.Net.Http.HttpClient": "Warning"
    }
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:3000" ]
  }
}
